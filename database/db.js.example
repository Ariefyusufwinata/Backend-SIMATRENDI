import mysql from 'mysql2';

const db = mysql.createConnection({
    host: '',
    database: '',
    user: '',
    password: '',
    port: 3306
});

db.connect((err) => {
    if (err) throw err;
    console.log('Connected to database');
});

export default db;